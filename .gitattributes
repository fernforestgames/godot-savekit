# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Only include addons/savekit when downloading from the Asset Library.
/**             export-ignore
/addons         !export-ignore
/addons/savekit !export-ignore
